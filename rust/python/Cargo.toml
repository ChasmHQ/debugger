[package]
name = "sevm-revm-python"
description = "Python bridge for the sevm REVM feasibility core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lib]
name = "_revm"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.29.2", features = ["abi3-py310", "extension-module"] }
revm.workspace = true
sevm-revm-core.workspace = true

