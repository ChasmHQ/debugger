[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sevm"
version = "0.1.0"
description = "sevm is an interactive EVM playground on Py-EVM, built for red-team dynamic analysis."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "sevm authors" }]
keywords = ["solidity", "evm", "ethereum", "debugger", "py-evm", "web3"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Topic :: Software Development :: Debuggers",
]

# Runtime dependencies. Every third-party package imported by the sources under
# src/sevm is declared here (not left to transitive resolution). `eth`, `eth_abi`,
# `eth_utils` and `eth_account` all arrive with web3, but sevm imports them directly,
# so they are named explicitly.
#
# The eth-tester stack is spelled out rather than taken as `web3[tester]`, whose
# `eth-tester[py-evm]` extra requires `eth-hash[pysha3]` -> safe-pysha3, a C extension
# with x86_64-only wheels: on any other machine it builds from source and needs a
# compiler. web3 itself already requires `eth-hash[pycryptodome]`, so that backend is
# there either way; it is named here so the one keccak implementation sevm relies on is
# not left to a transitive extra. The eth-tester range mirrors web3's own.
dependencies = [
    "web3>=7.0",
    "eth-tester>=0.13.0b1,<0.14.0b1",
    "py-evm>=0.12.0b2",
    "eth-hash[pycryptodome]>=0.5.1",
    "py-solc-x>=2.0",
    "textual>=8.0",
    "rich>=15.0",
    "pygments>=2.20",
    "eth-abi>=5.2",
    "eth-utils>=5.0",
    "eth-account>=0.13",
    "eth-keys>=0.6",
    "rlp>=4.0",
    "packaging>=23.0",
]

[project.scripts]
sevm = "sevm.cli:main"

[project.urls]
Homepage = "https://github.com/your-org/sevm"
Documentation = "https://github.com/your-org/sevm/blob/main/README.md"

# src layout: the package lives under src/. Hatchling includes package data
# files (e.g. tui/sevm.tcss) automatically.
[tool.hatch.build.targets.wheel]
packages = ["src/sevm"]

[dependency-groups]
# uv-native dev group: `uv sync` installs these by default.
dev = [
    "pytest>=8.0",
    "textual-dev>=1.8", # `textual console` for debugging the debugger
    "ruff>=0.9",
    "mypy>=1.13",
]

[tool.pytest.ini_options]
markers = ["network: hits the network; run with SEVM_NETWORK_TESTS=1"]
testpaths = ["tests"]
# Put tests/ on sys.path so the shared `harness` module imports cleanly, and rely
# on the installed (editable) `sevm` package for `import sevm.*` - no sys.path hacks.
pythonpath = ["tests"]
addopts = "-ra"

[tool.ruff]
line-length = 90
target-version = "py310"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "UP",  # pyupgrade (modernize typing: Dict->dict, Optional[X]->X | None)
    "SIM", # flake8-simplify
    "RUF", # ruff-specific
]
ignore = [
    "E501",   # line length is owned by the formatter; the rest are unsplittable strings
    "RUF012", # ClassVar for mutable class attrs fights Textual's BINDINGS/CSS idiom
]

[tool.ruff.lint.per-file-ignores]
# Unpacking a shared fixture tuple and using only some fields is idiomatic here.
"tests/**" = ["RUF059"]

[tool.mypy]
python_version = "3.10"
# Pragmatic, not strict: this wraps an untyped EVM stack (py-evm, web3) where full
# strictness would drown real signal in third-party Any-noise. Tighten over time.
warn_redundant_casts = true
warn_unused_ignores = true
ignore_missing_imports = true
